---
id: W03
title: 分层依赖声明协议设计（.harness/layers.yaml）
type: grilling
status: open
blocked_by: [W01]
assignee: ""
map: ../MAP-p1-机械执法层.md
---

## Question

基于 W01 的工具选型，敲定分层声明协议的全部设计：

- `.harness/layers.yaml` 的完整 schema（层定义、依赖方向、豁免清单、生效/退役条件字段）
- 4 种项目类型（cli/lib/web/notebook）各自的默认分层预置（如 cli 型：cli → core → _utils）
- 模板如何播种：进 `templates/common/.harness/` 还是按类型渲染
- 校验入口：接进 `make verify` 还是独立 `make check-layers`；校验失败的报错如何携带修复提示
- AGENTS.md 模板中对应的分层说明段落
- SPEC 增补条款草案（对应报告 P1 表"§2.4 分层规则"方向，措辞留给雾区里的 SPEC v2.1 票据）

前置：须先读 W01 结论与 `2026-09-01-harness-engineering-research.md` 中"分层依赖"相关一手证据。

## Resolution

（待解决）
