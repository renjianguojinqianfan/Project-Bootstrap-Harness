---
id: W04
title: 状态机推进契约设计（progress.json + 检查点）
type: grilling
status: open
blocked_by: []
assignee: ""
map: ../MAP-p1-机械执法层.md
---

## Question

5 阶段状态机（init→plan→execute→evaluate→done）是报告认定的"最可惜"资产：好抽象但没接线。用户已定形态：**播种契约 + 脚本，推进由用户的 Agent 运行时执行**。本票据敲定：

- `progress.json` 推进契约：哪些字段、什么条件下允许改 `stage`、谁能改（Agent 运行时）、改后留什么痕（`stage_history` 追加而非覆盖？）
- 播种脚本 `harness stage <name>`（或 Makefile target）的职责边界：只做校验与落盘，不做任何"执行"
- git 检查点：每次阶段推进自动打轻量 tag / 提示创建检查点分支？回滚辅助的形态（不越界前提下）
- `validate` 是否应校验 `progress.json` 的阶段合法性（如从 init 直接跳 evaluate 算不算违规）
- 与现有 `validators/progress_json.py` 的衔接

## Resolution

（待解决）
