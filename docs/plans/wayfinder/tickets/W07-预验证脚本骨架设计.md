---
id: W07
title: 预验证脚本骨架设计（scripts/verify/ + make add-verify）
type: grilling
status: open
blocked_by: []
assignee: ""
map: ../MAP-p1-机械执法层.md
---

## Question

报告维度 3 的核心缺口：`make verify` 只证明"代码能编译、测试能过"，没证明"功能是对的"。行业对标（Qoder verify skill / Anthropic Puppeteer 端到端路径）。敲定：

- `scripts/verify/` 骨架的结构：引导用户把核心用户路径编码成可执行验证脚本的模板（如 `verify_smoke.py` 占位 + 编写指南）
- `make add-verify` 的行为：新建一个验证脚本骨架？命名约定？
- `make verify` 与 `scripts/verify/*` 的集成方式：默认全跑还是独立 `make verify-e2e`（注意本地门禁 <5 秒的行业建议——重验证不应拖慢快门禁）
- 4 种项目类型的默认验证预置（cli 型：`--help` 冒烟；web 型：启动+健康检查等）
- 顺带决策（雾区项毕业到此）：**lint 报错注入修复指令**是否在此实现——`make verify` 失败输出附加项目语义的修复提示（引用 AGENTS.md 对应段落），还是归属 W03
- 生效/退役条件（可拆卸原则）

## Resolution

（待解决）
